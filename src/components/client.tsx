'use client';

import * as fm from 'framer-motion';

export const MotionDiv = fm.motion.div;
export const MotionButton = fm.motion.button;
export const AnimatePresence = fm.AnimatePresence;
export * from 'swiper/react';
