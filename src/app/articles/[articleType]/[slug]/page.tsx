import type {ReactNode} from 'react';

interface PageProps {
    children: ReactNode;
}

const Page = (props: PageProps) => {
    return (
        <div className="">

        </div>
    );
};

export default Page;